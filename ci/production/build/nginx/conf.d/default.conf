# upstream backend {
#   server backend:3000;
# }

server {
  listen 80 default_server;
  access_log off;

#   location /api {
#     proxy_pass http://backend;
#   }

  location / {
    root /etc/nginx/static/frontend;
  }
}
